global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "conduit"

    metrics_path: /metrics

    static_configs:
      - targets:
          - api1:8080
          - api2:8080
          - api3:8080
